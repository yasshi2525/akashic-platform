# About

Akashic Engine のゲーム実行に必要なサーバー実装です。

すべてのプレイのアクティブインスタンスを実行する専用サーバーです。

## NOTE

> [!NOTE]
>
> サーバーは Tick イベントを毎フレーム発生させる。現状はその都度 `akashic-storage` に送信しているが、通信負荷を考慮すると、まとめて送信する方がよいかもしれない。
>
> 現状、1アクティブインスタンスごとにソケットを持ってしまっているので、使い回すように修正する
>
> playId の採番状況を永続化し、重複しないようにする
